extends layout
block content

	h1 #{movie.title} (#{movie.release_year_formatted})

	p #[strong Release date: ] #{movie.release_date_formatted}

	p #[strong Director: ]
		a(href= movie.director.url, title= movie.director.name) #{movie.director.name}

	p #[strong Plot: ] !{movie.plot_synop}

	hr
	p
		a(href=movie.url + '/delete') Delete movie
	p
		a(href=movie.url + '/update') Update movie